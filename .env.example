# Copy to .env (gitignored); Task loads this file automatically.
RUNPOD_API_KEY=
# Live read-only tests also require this explicit opt-in. Leave disabled normally.
RUNPOD_LIVE_TESTS=0
# Optional alternate host; blank values fall back to production.
# RUNPOD_API_BASE_URL=https://api.runpod.io
