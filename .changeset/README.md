# Changesets

Run `pnpm changeset` for a change that should be released. Choose the package,
version bump, and a short description of the user-visible change, then commit
the generated Markdown file with the code.

See [the release guide](../docs/releases.md) for versioning and review rules.
